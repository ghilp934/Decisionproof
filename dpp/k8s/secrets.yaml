# IMPORTANT: This is a TEMPLATE file
# DO NOT commit actual secrets to git
# Use AWS Secrets Manager or Kubernetes Secrets in production

# To create secrets from AWS Secrets Manager:
# kubectl create secret generic dpp-secrets \
#   --namespace=dpp-production \
#   --from-literal=database-url="postgresql://dpp_user:${DB_PASSWORD}@prod-db.example.com:5432/dpp" \
#   --from-literal=redis-url="redis://prod-redis.example.com:6379/0" \
#   --from-literal=redis-password="${REDIS_PASSWORD}" \
#   --from-literal=sentry-dsn="${SENTRY_DSN}"

# Or use external-secrets operator to sync from AWS Secrets Manager:
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: aws-secrets-manager
#   namespace: dpp-production
# spec:
#   provider:
#     aws:
#       service: SecretsManager
#       region: us-east-1
#       auth:
#         jwt:
#           serviceAccountRef:
#             name: dpp-external-secrets
# ---
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: dpp-secrets
#   namespace: dpp-production
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: SecretStore
#   target:
#     name: dpp-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: database-url
#       remoteRef:
#         key: dpp/production/database-url
#     - secretKey: redis-url
#       remoteRef:
#         key: dpp/production/redis-url
#     - secretKey: redis-password
#       remoteRef:
#         key: dpp/production/redis-password
#     - secretKey: sentry-dsn
#       remoteRef:
#         key: dpp/production/sentry-dsn

---
apiVersion: v1
kind: Secret
metadata:
  name: dpp-secrets
  namespace: dpp-production
type: Opaque
stringData:
  # TEMPLATE ONLY - Replace with actual values or use external-secrets
  database-url: "postgresql://dpp_user:REPLACE_WITH_PASSWORD@prod-db.example.com:5432/dpp"
  redis-url: "redis://prod-redis.example.com:6379/0"
  redis-password: "REPLACE_WITH_REDIS_PASSWORD"
  sentry-dsn: "REPLACE_WITH_SENTRY_DSN"
